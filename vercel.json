{
    "version": 2,
    "builds": [
      {
        "src": "main.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/list/(.*)", "dest": "/list/$1" },
      { "src": "/hero/(.*)", "dest": "/hero/$1" },
      { "src": "/(.*)", "dest": "main.js" }
    ]
  }